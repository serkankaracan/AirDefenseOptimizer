<UserControl x:Class="AirDefenseOptimizer.Views.AboutWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AirDefenseOptimizer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFF">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Başlık -->
            <TextBlock Text="Hava Savunma Optimizasyon Sistemi" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#333"
                       HorizontalAlignment="Center" />

            <!-- Alt Başlık -->
            <TextBlock Text="Proje Tasarımı ve Geliştiren" 
                       FontSize="18" 
                       Margin="0,20,0,10" 
                       HorizontalAlignment="Center" />

            <!-- İsim ve Unvan -->
            <TextBlock Text="İsmail CIRRIK" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2E8B57"
                       HorizontalAlignment="Center" />

            <TextBlock Text="Bilgisayar Mühendisi" 
                       FontSize="16" 
                       Foreground="#666" 
                       HorizontalAlignment="Center" />

            <!-- Alt Metin -->
            <TextBlock Text="Bu yazılım, hava savunma sistemleri optimizasyonu alanında İsmail CIRRIK'in yüksek lisans tezi kapsamında tasarlanmış olup, İsmail CIRRIK tarafından kodlanmıştır."
                       FontSize="14" 
                       TextAlignment="Center" 
                       TextWrapping="Wrap" 
                       Margin="20" 
                       Foreground="#555" />
        </StackPanel>
    </Grid>
</UserControl>
